import React from "react";
//import { useState } from "react";
import Card from "./FilterCard";

const Filters = () => {
  return (
    <div className="flex">
      <Card />
    </div>
  );
};

export default Filters;
